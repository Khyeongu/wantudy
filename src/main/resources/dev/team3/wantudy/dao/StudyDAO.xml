<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.team3.wantudy.dao.StudyDAO">
	<select id="getStudy" parameterType="int" resultType="StudyDTO">
		select * 
		from study
		where no = #{study_no}
	</select>
	
	
	<select id="createStudy" parameterType="StudyDTO">
		insert into study
		(no, name, content, startdate, enddate, capacity, member_no, category_no)
		values
		(STUDY_NO_SEQ.nextval, #{content}, #{startdate}, #{enddate}, #{capacity}, #{member_no}, #{category_no})
	</select>
</mapper>
